# OpenWeatherMap API key (free tier: https://openweathermap.org/api)
# Required for real weather forecasts. Without it, the /weather endpoint
# returns placeholder data so the app still works during development.
OPENWEATHER_API_KEY=your_api_key_here
